apiVersion: v1
kind: Service
metadata:
  name: drop
  labels:
    app.kubernetes.io/name: drop
    app.kubernetes.io/component: relay
    app.kubernetes.io/part-of: drop
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: drop
    app.kubernetes.io/component: relay
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
